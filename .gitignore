# Dependencies
node_modules/
.pnpm-store/

# Build outputs
.svelte-kit/
build/
dist/

# Environment files
.env
.env.local
.env.*.local
!.env.example

# Logs
logs/
*.log

# Test artifacts
test-results/
playwright-report/
coverage/
.lighthouseci/
bundle-report.json

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# direnv + Nix
.direnv/
result
result-*

# Bazel — only the root-level workspace symlinks bazel itself creates.
# Anchored with leading / so scripts/bazel-*.sh wrappers stay tracked.
/bazel-*
/bazel-bin/
/bazel-out/
/bazel-testlogs/
/bazel-site.scaffold/
.cache/bazel-*

# AI/LLM tooling (CLAUDE.md and AGENTS.md are tracked house contracts)
.claude/
.serena/
.aider*
# Local dev artifacts
.huskycat/
.gstack/

# Tofu local-override (operator-side rustfs port-forward, never commit)
infra/tofu/backend.local.hcl
infra/tofu/darkmap.tfplan

# Tofu init/state artifacts (docs/CI-SCHEMA.md §7; state belongs in env-owned S3, not the repo)
infra/tofu/.terraform/
infra/tofu/.terraform.lock.hcl
**/*.tfstate
**/*.tfstate.backup

# Regenerable UI captures (just capture-shipped-ui / capture-breakpoints)
docs/ux/public-readiness/assets/shipped/
