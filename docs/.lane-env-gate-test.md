# Lane-env gate test marker

This file's only purpose is to give a PR something to touch so the
`lane-env.yml` workflow fires and we can confirm the
`vars.BLAHAJ_LANE_ENV_ENABLED == 'true'` gate causes the job to skip
cleanly (not error) when the var is unset.

Delete after the validation PR closes — `docs/playbook/scaffold-v1-upgrade.md`
documents the expected behavior for future spokes.
